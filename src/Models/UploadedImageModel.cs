namespace LetsGoSEA.WebSite.Models
{
    public class UploadedImageModel
    {
        // Unique identifier
        public string UploadedImageId { get; set; } = "Default";

        // Holds the uploaded image name
        public string UploadedImageName { get; set; } = "Default";

        // Holds the uploaded image path
        public string UploadedImagePath { get; set; } = "Default";

    }
}